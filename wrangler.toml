name = "redwinter-online-test"
main = "worker.js"
compatibility_date = "2026-08-19"

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = ["/api/*"]

# After deploying the Worker, attach your custom domain in Cloudflare and
# point the GoDaddy-managed DNS record at the Cloudflare target Cloudflare gives you.
